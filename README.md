# Content-Inventory
A modular Python tool for building a **content inventory** from mixed document sources (TXT, MD, HTML, XML, DOCX, PDF).   It extracts **summaries**, **entities**, **sentiment**, and **duplicates**, then exports structured data to **SQLite**, **CSV**, and **Markdown** reports.
